# linuxmetrics-logstash-client


```
$ ./bin/linuxmetrics-logstash -h
Usage of ./bin/linuxmetrics-logstash:
  -c string
    	Configuration file
  -console
    	Console output
  -host string
    	Logstash hostname
  -interval int
    	Seconds between samples (default 1)
  -port int
    	Logstash port (default -1)
  -proc-path string
    	Linux proc path
```
